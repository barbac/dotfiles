# vim: set filetype=gitconfig:
[color]
	ui = auto
[core]
	excludesfile = ~/.gitignore_global
	autocrlf = input
	editor = vim
[push]
	default = simple
[credential]
	helper = cache --timeout=108000 #30 hrs
[alias]
	pu = !"git fetch origin -v; git fetch upstream -v; git merge upstream/master;"
[include]
        path = ~/dotfiles/git_extra
